# recursive-artifact-engine
AI artifact-orchestration system where generated state is never trusted merely because the model says it is correct. Independent verification, provenance, and anti-laundering circuit breaker for multi-format documents (DOCX/PDF/PPTX/XLSX).
